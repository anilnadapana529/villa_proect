<IfModule mod_rewrite.c>
    RewriteEngine On

    # Exclude web folder from rewrite
    RewriteCond %{REQUEST_URI} !^/web/

    # Redirect all requests to public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
