<IfModule mod_rewrite.c>
    RewriteEngine On

    # API requests go to api.php
    RewriteRule ^api/(.*)$ api.php [L,QSA]

    # Allow direct access to PHP files
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Default to home.php
    RewriteRule ^$ home.php [L]
</IfModule>
