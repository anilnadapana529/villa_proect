FILES TO UPLOAD TO YOUR SERVER
===============================

Upload these 6 files to fix all database and security issues:

1. App/Models/Admin.php
   → Fixed table name from "admins" to "admin"

2. App/Models/Booking.php
   → Added owner_id logic when creating bookings

3. App/Models/Villa.php
   → Fixed SQL injection vulnerabilities
   → Now uses prepared statements

4. App/Models/VillaImages.php
   → Fixed SQL injection vulnerabilities
   → Now uses prepared statements

5. App/Models/OwnerStats.php
   → Fixed SQL injection vulnerabilities
   → Now uses prepared statements

6. App/Models/UserStats.php
   → Fixed SQL injection vulnerabilities
   → Now uses prepared statements

AFTER UPLOADING FILES:
======================

Run this SQL in phpMyAdmin to add the missing owner_id column:

ALTER TABLE bookings
ADD COLUMN owner_id INT DEFAULT NULL AFTER villa_id;

(Full SQL is in database_fixes.sql)

Then test admin login at:
https://topmost.in/api-test.html
